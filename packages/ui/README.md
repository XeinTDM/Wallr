# Wallr UI

The shared UI component library for the Wallr workspace. 

This crate implements Wallr's premium, glassmorphic design system using custom CSS (no Tailwind) and `lucide-dioxus` icons. These components are strictly shared across the web, desktop, and mobile clients.
